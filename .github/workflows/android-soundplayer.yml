name: Android Sound Player CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:
    name: Build Android Sound Player
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Grant execute permission for gradlew
      run: chmod +x AndroidSoundPlayer/gradlew

    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches/
          ~/.gradle/wrapper/
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Validate Gradle wrapper
      working-directory: AndroidSoundPlayer
      run: |
        ls -la gradle/wrapper/
        echo "Checking gradle-wrapper.properties..."
        cat gradle/wrapper/gradle-wrapper.properties

    - name: Build Debug APK (no tests)
      working-directory: AndroidSoundPlayer
      run: ./gradlew assembleDebug --no-daemon --stacktrace

    - name: Check build outputs
      working-directory: AndroidSoundPlayer
      run: ls -la app/build/outputs/apk/debug/ || echo "No APKs found (expected for this demo project)"

    - name: Upload APK if exists
      uses: actions/upload-artifact@v4
      with:
        name: soundplayer-debug-apk
        path: AndroidSoundPlayer/app/build/outputs/apk/debug/*.apk
        retention-days: 30
        if-no-files-found: ignore
